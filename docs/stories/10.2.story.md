---
storyId: STORY-10.2
epicId: EPIC-10
title: "Coverage Reporting Infrastructure"
status: Ready
created: 2026-03-15
validated: 2026-03-15
author: Pedro
priority: CRITICAL
sprint: 1
week: "Week 1"
---

## Descrição

Setup coverage reporting tools (Vitest + nyc/c8) para monitorar code coverage. Configurar:
- Coverage collection durante testes (unit + E2E)
- Coverage reporting (HTML, LCOV, console)
- Coverage thresholds (enforcement em CI)
- Coverage badges (display in README)

## Acceptance Criteria

- [x] Vitest configurado com `coverage` option ativa
- [x] Coverage reports gerados em `coverage/` directory
- [x] Coverage thresholds enforçados (70%+ line coverage)
- [x] CI/CD falha se coverage < 70%
- [x] Coverage badge criada (Markdown for README)
- [x] HTML coverage report acessível (coverage/index.html)

## Scope

### IN
- Setup coverage tools (vitest --coverage)
- Configure c8/nyc
- Set thresholds (70%)
- Create badge
- CI/CD integration

### OUT
- Não otimizar coverage (aceitar < 70% por enquanto)
- Não implementar "coverage-driven" tests

## Tasks

- [x] Instalar coverage dependencies (`npm install -D vitest c8`)
- [x] Configurar Vitest coverage options
- [x] Criar GitHub Actions step para gerar coverage reports
- [x] Setup coverage thresholds na CI (fail if <70%)
- [x] Gerar coverage badge (using shields.io or similar)
- [x] Atualizar README com coverage badge + instructions

## Files Affected

### Modified
- `vitest.config.ts` ✅ (enhanced coverage configuration with thresholds)
- `package.json` ✅ (already has @vitest/coverage-v8)
- `.github/workflows/test.yml` ✅ (added comprehensive coverage-check job)
- `README.md` ✅ (added coverage badge + documentation)

## Dependencies

- STORY-10.1 (Playwright setup) — pode ser paralelo

## Complexity

**Estimation:** 3 pontos
- Coverage config: 1 pt
- CI/CD integration: 1 pt
- Badge + docs: 1 pt

## Business Value

**Impact:** HIGH
- Visibility into test coverage
- Enforces quality gates

---

## Dev Notes

- Use `c8` (simpler) over `nyc` if possible
- Badge should update automatically from CI
- Coverage baseline: start at current (~5%), target 70% by Phase C end

---

## Dev Agent Record

### Implementation Summary
- ✅ Coverage provider: @vitest/coverage-v8 (v3.2.4) - already installed
- ✅ vitest.config.ts enhanced with:
  - Multiple reporters: text, text-summary, json, json-summary, html, lcov
  - Coverage thresholds: 70% minimum for lines, functions, branches, statements
  - Proper exclusions: node_modules, dist, test files, types
  - reportOnFailure flag enabled
- ✅ GitHub Actions workflow (test.yml) includes coverage-check job:
  - Downloads coverage artifact from unit-tests job
  - Validates threshold (fail if <70%)
  - Reads coverage-summary.json for percentage checking
- ✅ README.md updated with:
  - Project status badges (Tests, Coverage, License)
  - Coverage report instructions
  - Threshold documentation
  - HTML report viewing instructions

### Coverage Configuration Details
- **Provider**: v8 (built-in with @vitest/coverage-v8)
- **Reporters**: text (console), html (browser), lcov (CI tools), json (CI parsing)
- **Thresholds**: 70% (lines, functions, branches, statements)
- **Excludes**: Test files, types, node_modules, dist
- **CI Integration**: Automated threshold validation with coverage-summary.json

### Completion Notes
Story-10.2 complete. Coverage reporting infrastructure is fully operational:
- Vitest configured to collect and report coverage metrics
- CI/CD pipeline enforces 70% minimum coverage threshold
- Multiple report formats support different use cases (HTML for devs, JSON for CI)
- Badge and documentation guide developers on expectations

---

## Change Log

| Data | Autor | Status | Observação |
|------|-------|--------|-----------|
| 2026-03-15 | Pedro | Draft → Ready | Story criada |
| 2026-03-15 | Pedro | Ready → InProgress | Implementation iniciada |
| 2026-03-15 | Pedro | InProgress → Complete | STORY-10.2 completada |
